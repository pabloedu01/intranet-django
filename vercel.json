{
    "version":2,
    "builds": [{
        "src": "vercel_app/wsgi.py",
        "use": "@avercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime":"python3.9"}
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "vercel_app/wsgi.py"
        }
    ]
}
