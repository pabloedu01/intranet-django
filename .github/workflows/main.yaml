name: SSH Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Install SSH client
        run: ls
      - name: install ssh
        run: sudo apt-get install -y openssh-client
      - name: connect_ssh
        run: ssh -l ${{ secrets.SSH_HOST }} localhost